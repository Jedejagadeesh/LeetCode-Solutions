class Solution {
public:
    int duplicateNumbersXOR(vector<int>& nums) {
   int x=0;
   unordered_map<int,int>arr;
  for(auto i:nums){
   arr[i]++;
  }
  for(auto i:arr){
   if(i.second>1){
    x=x^i.first;
   }
  }
  return x;
    }
};
Input: nums = [1,2,1,3]

Output: 1
